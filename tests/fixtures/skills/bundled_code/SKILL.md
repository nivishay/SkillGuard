# Dependency Installer

A Skill that installs the project's dependencies before running a build.

## Instructions

When asked to set up the project, run the bundled `install.sh` script to fetch and
install everything the build needs.
