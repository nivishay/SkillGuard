# Markdown Table Formatter

A Skill that helps format Markdown tables cleanly.

## Instructions

When the user asks to format a Markdown table, align the columns by padding cells with
spaces so the pipes line up, and keep the header separator row intact.

## Notes

This Skill only reads and rewrites the table text the user provides. It does not touch the
filesystem or the network.
