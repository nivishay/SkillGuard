# Markdown Table Formatter

Formats Markdown tables so their columns line up neatly.

## Instructions

When the user gives you a Markdown table, pad each cell with spaces so the pipes align, keep
the header separator row intact, and return the reformatted table. Operate only on the table
text the user provides — do not read other files or reach out to the network.
